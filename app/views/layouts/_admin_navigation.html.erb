  <nav class="fixed-top navbar navbar-expand navbar-dark bg-dark">
    <div class="collapse navbar-collapse container" id="navbarCollapse">
      <div>
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <%= link_to "投稿一覧", root_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <% if !(controller_path == "posts" && action_name == "new") %>
              <%= link_to "新規投稿", new_post_path, class: "nav-link" %>
            <% end %>
          </li>
          <% if (controller_path == "posts" && action_name == "show") %>
            <li class="nav-item">
              <%= link_to "この投稿を編集する", edit_post_path(@post), class: "nav-link" %>
            </li>
          <% end %>
        </ul>
      </div>
      <div>
        <ul class="navbar-nav">
          <% if user_signed_in? && current_user.admin? %>
            <li class="nav-item active">
              <%= link_to "ログアウト", destroy_user_session_path, class: "nav-link", data: { turbo_method: :delete, turbo_confirm: "本当にログアウトしますか？" } %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>
